import { BRAND_PALETTE } from "@/lib/brand-palette";

/**
 * Les e-mails sont rendus en theme clair : un client de messagerie n'a ni
 * variables CSS ni preference de theme a nous donner.
 */
export const EMAIL_COLORS = BRAND_PALETTE.light;
